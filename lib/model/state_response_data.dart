

import 'dart:convert';

List<GetStateModel> getStateModelFromJson(String str) => List<GetStateModel>.from(json.decode(str).map((x) => GetStateModel.fromJson(x)));

String getStateModelToJson(List<GetStateModel> data) => json.encode(List<dynamic>.from(data.map((x) => x.toJson())));

class GetStateModel {
  GetStateModel({
    required this.stateId,
    required this.stateName,
  });

  int stateId;
  String stateName;

  factory GetStateModel.fromJson(Map<String, dynamic> json) => GetStateModel(
    stateId: json["state_id"],
    stateName: json["state_name"],
































































































































































































































































































































































































































































  );

  Map<String, dynamic> toJson() => {
    "state_id": stateId,
    "state_name": stateName,
  };
}
